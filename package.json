{
  "name": "transaction-typeorm",
  "version": "0.0.1",
  "description": "The transaction base on the typeorm",
  "author": "",
  "license": "ISC",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": ["lib/**/*"],
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "dependencies": {
    "rxjs": "^7.8.0",
    "typeorm": "^0.3.12"
  },
  "devDependencies": {
    "@types/node": "^18.14.3",
    "typescript": "^4.9.5"
  }
}
